Var * CellMin(Var * v);
Var * CellMax(Var * v);

Var * Add(Var * l, Var * r);
Var * Sub(Var * l, Var * r);
Var * Mul(Var * l, Var * r);
Var * DivInt(Var * l, Var * r);
Var * Mod(Var * l, Var * r);

Var * CellOp(InstrOp op, Var * left, Var * right);

